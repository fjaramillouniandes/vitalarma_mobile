<?xml version="1.0" encoding="utf-8"?>

<com.google.android.material.bottomnavigation.BottomNavigationView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottomNavigation"
    android:layout_width="match_parent"
    android:layout_height="@dimen/bottom_navigation_height"
    android:minHeight="@dimen/bottom_navigation_height"
    android:background="@color/surface_container"
    app:itemBackground="@drawable/bottom_nav_item_background"
    app:itemIconSize="24dp"
    app:itemIconTint="@color/primary"
    app:itemTextAppearanceActive="@style/TextAppearance.Vitalarma.BottomNavigation"
    app:itemTextAppearanceInactive="@style/TextAppearance.Vitalarma.BottomNavigation"
    app:itemTextColor="@color/primary"
    app:labelVisibilityMode="labeled"
    app:menu="@menu/menu_bottom_navigation" />